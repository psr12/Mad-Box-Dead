/// @description on beat

//reduce counter


if counter == 0 
{
	counter = howlong;
}
if counter ==1 woah = woahthere

counter--;

if counter >0 yscale *= 1.2
else {yscale *= 0.1; isblue = -isblue;}

notyet = 0; //stop checking for scale, done in editor