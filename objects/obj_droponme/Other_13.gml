/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

if place_meeting(x,y, obj_MadSquare)
{
	var the = instance_create_depth(tx, ty, depth, it);
	the.image_xscale = xsize
	the.image_yscale = ysize
	the.image_angle = image_angle;
	instance_destroy()
}