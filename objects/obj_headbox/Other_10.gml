/// @description on beat
// You can write your code in this editor

// Inherit the parent event
event_inherited();



beats_until_attack--;
wscale *= 1.5

if !beats_until_attack
{
	var baby = instance_create(x,y+100, obj_vulnhead)
	//var scool = scale;
	with baby {image_scale(8) }
	instance_destroy();
}
