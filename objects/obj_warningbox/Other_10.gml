/// @description on beat
// You can write your code in this editor

// Inherit the parent event
event_inherited();



beats_until_attack--;
wscale *= 1.5

if !beats_until_attack
{
	var baby = instance_create(x,y+100, obj_talon)
	//var scool = scale;
	with baby {image_scale(4) }
	instance_destroy();
}
