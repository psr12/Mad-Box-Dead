/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

	if !finish with obj_recorder event_user(8)
	mask_index = -1;
	finish = true;

	with obj_artyhandler {
		scr_changedummyseq(0, ch_shocked)
	}
