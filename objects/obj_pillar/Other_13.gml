/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

//just fall down

if justonce {
	justonce = false;
	waittill += offset; life += offset;}

timer++; 
if timer > waittill {
	if justagain {justagain = false; if obj_MadSquare.y > y {scaleto = -scaleto}
	}
	image_yscale = lerp(image_yscale, scaleto, 0.2)
	y = ystart - sprite_height/2
	
	if place_meeting(x,y,obj_MadSquare) scr_killmadrat()
}

life--;
if !life instance_destroy();